import 'package:flutter/material.dart';

class MapRide extends StatelessWidget {
  const MapRide({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Hola'),
    );
  }
}
